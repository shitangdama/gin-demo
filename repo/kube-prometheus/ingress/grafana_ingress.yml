apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: grafana
  namespace: monitoring
  annotations:
    kubernetes.io/ingress.class: "nginx"
    # cert-manager.io/issuer: "letsencrypt-prod-monitoring"
    # certmanager.k8s.io/acme-challenge-type: http01
spec:
  # tls:
  # - hosts:
  #   - prometheus.shitangdama.cn
  #   secretName: prometheus-k8s
  rules:
    - host: grafana.shitangdama.cn
      http:
        paths:
        - path: /
          backend:
            serviceName: grafana
            servicePort: 3000